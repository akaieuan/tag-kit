{
  "name": "@tag-kit/example-moderation",
  "version": "0.1.0",
  "private": true,
  "description": "Example tag catalog for content moderation. Drops straight into a moderation app — text/image/video/audio/cross-modal scopes.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests for example catalogs'",
    "build": "echo 'examples are source-only — no build step'",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@tag-kit/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
